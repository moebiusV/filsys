# --- build artifacts (generated by ./configure and make; NOT shipped) ---
mount.filsys
findfs.filsys
mkfs.filsys
fsck.filsys
*.o
*.a
/Makefile
config.log
config.status
config.h
filsys.pc
stamp-h1
.deps/
.dirstamp
autom4te.cache/
*.tar.gz
*.tar.xz
*.tar.bz2

# --- disk images (large binaries; fetch with ./fetch.sh) ---
rp06-0.disk
rp06-0.disk.gz
tm0.tap

# --- local libfuse3 dev package + extracted headers (fetch with ./fetch.sh) ---
libfuse3-dev_*.deb
fuselib/

# --- scratch / tooling ---
.venv/
__pycache__/
mnt/
mount.log
testcopy.disk
ls-off
core
core.*
*~

# --- automake test harness artifacts (make check) ---
test_matrix
test-suite.log
*.log
*.trs

# --- local notes (never pushed to the remote) ---
docs/
