# --- build artifacts (generated by ./configure and make; NOT shipped) ---
mount.filsys
findfs.filsys
mkfs.filsys
fsck.filsys
*.o
*.a
/Makefile
config.log
config.status
config.h
filsys.pc
stamp-h1
.deps/
.dirstamp
autom4te.cache/
*.tar.gz
*.tar.gz.asc
*.tar.xz
*.tar.bz2

# --- disk images (large binaries; fetch with ./fetch.sh) ---
rp06-0.disk
rp06-0.disk.gz
tm0.tap

# --- local libfuse3 dev package + extracted headers (fetch with ./fetch.sh) ---
libfuse3-dev_*.deb
fuselib/

# --- local libfuse2 dev package + extracted headers (for --with-fuse=fuse2) ---
libfuse-dev_*.deb
fuselib2/

# --- scratch / tooling ---
.venv/
__pycache__/
mnt/
mount.log
testcopy.disk
ls-off
core
core.*
*~

# --- SIMH validation artifacts (downloaded images, SIMH source, and System V
#     proprietary source -- validate only, never vendor) ---
simh/
test_matrix_*.img

# --- automake test harness artifacts (make check) ---
test_matrix
test_oracle
test_fuse_unlink_open
test-suite.log
*.log
*.trs

# --- local notes (never pushed to the remote) ---
docs/
qemu-images/
