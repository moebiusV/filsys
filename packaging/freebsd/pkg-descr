A FUSE driver and set of disk utilities for Research Unix (V4 through
V7, and 32V) filesystem images on a modern host.  Ships mount.filsys
(the FUSE mount), findfs.filsys (superblock/partition location),
mkfs.filsys (create a filesystem), and fsck.filsys (check one).
